# Plan de Trabajo: Listado de Súper Recetas

## Estado Actual
- [x] Completado

## Rama
`feat/super-recipe-list`

## Tareas
- [x] Añadir métodos de `update` y `delete` al `superRecipeApi` en el frontend.
- [x] Crear el componente `SuperRecipeList` que implemente un listado con búsqueda, paginación (10 elementos) y un empty state.
- [x] Crear el componente `SuperRecipeManager` para administrar los estados de listado y creación (builder).
- [x] Actualizar `App.jsx` para usar `SuperRecipeManager` en lugar del builder directo en la pestaña `superRecipe`.
- [x] Verificar build y tests.
